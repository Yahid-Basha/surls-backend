# Redis configuration for EC2 micro instance
# Replace with your actual EC2 private IP

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/short_url

# Redis on EC2 (replace with your EC2 private IP)
REDIS_HOST=10.0.1.123          # Your EC2 private IP
REDIS_PORT=6379
REDIS_SSL=false                # No SSL needed for internal communication
# REDIS_PASSWORD=your_password # Uncomment if you set a password

# AWS Cognito (for JWT validation only)
AWS_REGION=eu-north-1
COGNITO_USER_POOL_ID=eu-north-1_YM8z1VDH8
COGNITO_CLIENT_ID=4na18bij0f3nu5mipg1m8fmd6q